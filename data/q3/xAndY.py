import re
import commands
import os
import glob
import subprocess

def make(train,predictor):
    
    for line in open(pred):
        
        for line2 in open(train):
            
            if ((line in line2) and (line2 not in x)):
                x.write(line2+"\n")
            if ((line1 not in line2) and (line2 not in y)):
                y.write(line
